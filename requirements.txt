asgiref==3.12.1
Django==6.1
sqlparse==0.6.0
tzdata==2026.3
requests>=2.31,<3

# Postgres driver -- used when DATABASE_URL is set (e.g. on Vercel);
# local development keeps using SQLite and never imports this.
psycopg[binary]>=3.1,<4
